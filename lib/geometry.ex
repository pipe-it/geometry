defmodule Geometry do
  def read_integer(prompt) do
    IO.gets(prompt) |> String.trim() |> String.to_integer()
  end

  def polygon do
    # @type: triangle
    # @type: square
    # @type: circle
  end
end
